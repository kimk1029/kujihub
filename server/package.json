{
  "name": "kujihub-server",
  "version": "0.0.1",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "npx playwright install chromium 2>/dev/null; npx prisma db push --accept-data-loss && node index.js",
    "dev": "node index.js",
    "db:setup": "prisma migrate dev --name init",
    "db:deploy": "prisma migrate deploy",
    "db:seed": "node prisma/seed.js",
    "db:reset": "prisma migrate reset --force",
    "db:studio": "prisma studio"
  },
  "prisma": {
    "seed": "node prisma/seed.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "cheerio": "^1.0.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0",
    "playwright": "^1.58.2",
    "playwright-core": "^1.58.2"
  },
  "devDependencies": {
    "prisma": "^5.22.0"
  }
}
